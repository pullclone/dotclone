# USBGuard policy
# Replace with a generated ruleset (see scripts/usbguard-generate-policy.sh)
# Default: allow already-present devices; refine before enabling in production.
# Example format:
# allow id 1234:abcd name "Example USB Device" with-interface 08:*:* parent-hub "usb1"
#
# Placeholder permissive rule:
allow with-interface all-of {  }
